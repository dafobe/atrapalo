//Needed 
if (module.hot) {
  module.hot.accept()
}

//import layout
//import canvas (board)
//import brushes

//add canvas to layout
//add brushes to canvas

//init application


const name = `Atrapalo`;

console.log(`application ${name} entrypoint`);

GraphComponent.init();