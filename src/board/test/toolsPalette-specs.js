import {expect} from 'chai';
import {ToolsPalette} from '../models/toolsPalette.js';

describe('Tools Palette Module logic', () => {

  describe('create new ToolsPalette instance', () => {

    it('call without params', () => {
      const result = 'ok';
      expect(result).to.equal('ok');
    });
/*
    it('call with params', () => {
      const name = 'User';
      const result = getHello(name);

      expect(result).to.equal(`Hello dear ${name}`);
    });
    */
  });
});